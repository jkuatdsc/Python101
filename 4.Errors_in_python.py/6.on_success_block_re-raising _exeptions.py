"""
def GetStudentMarks(student)

    try:
       return (CalculateMarks(student.grades))
    except KeyError
        print('some message')
        raise

    finally
        print('The function passed successfully')

# Or

try:
    except

    else: # Run something in case an error does not occur

# raise keyword is used to raise exceptions instead of silencing them

"""
